{
  "admin": {
    "type": "object",
    "properties": {
      "action": { "type": "string",
                  "enum": ["approved", "cancelled"]
                }
    },
    "required": [ "action" ]
  }
}
